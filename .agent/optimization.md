ЧАСТЬ 1: ФРОНТЕНД (Next.js 16 + React 19)
Чеклист
#	Модуль / Слой	Описание узкого горла	Последствия	Рекомендация	Статус
1	Bundle / Code Splitting	Нет ни одного next/dynamic — AppChartWidget, AppColorGrid, AppDataTable грузятся статически	Первоначальная загрузка +200–400 KB JS (Recharts + DataGrid)	Обернуть тяжёлые компоненты в next/dynamic({ ssr: false }) на страницах дашборда	✅ Готово (5 страниц с AppChartWidget, 2 с AppColorGrid)
2	React RSC	250+ файлов с "use client" — весь app рендерится на клиенте	Большой JS-бандл, нет streaming, нет SSR для статичных данных	Аудит: убрать "use client" там, где нет useState/browser events; страницы-листинги — на Server Component	⏭️ Пропущено (масштаб слишком большой; архитектурное решение на уровне команды)
3	HTTP Client	Нет AbortSignal / timeout в http-client.ts	При быстрой навигации между вкладками "зависшие" запросы продолжают выполняться и обновляют stale-данные	Добавить AbortController в клиент; TanStack Query автоматически передаёт сигнал при наличии	✅ Готово (AbortError больше не глотается; signal из RequestInit пробрасывается в fetch)
4	AppSearchableSelect	searchable-select.tsx:115 рендерит весь filtered массив без виртуализации	При 1000+ клиентов браузер зависает при открытии дропдауна	Внедрить @tanstack/react-virtual или react-window для списка опций	✅ Готово (@tanstack/react-virtual, overscan=5, estimateSize=44px)
5	AppColorGrid	color-grid.tsx:146 — [...filteredRows].reverse().map(...) создаёт новый массив и рендерит все ячейки	При шахматке 300+ квартир — лаг 1–2 сек при фильтрации	Виртуализация строк + React.memo для ячеек	✅ Готово (reversedRows мемоизирован, GridCell=React.memo, handleCellClick=useCallback)
6	TanStack Query — рефреш	refetchInterval: 5 * 60_000 в use-full-dashboard-query.ts и use-overdue-payments.query.ts	При многих открытых вкладках — постоянная нагрузка на API каждые 5 мин	Убрать refetchInterval или увеличить до 15–30 мин	✅ Готово (изменено на 15 мин в обоих файлах)
7	Изображения	unit-photo-manager.tsx:92 и photo-lightbox.tsx:132 — сырые <img> с eslint-disable	Нет lazy-load, нет WEBP/AVIF конвертации, нет адаптивного ресайза	Использовать next/image с кастомным loader для API-URL	⚠️ Приемлемо (требует конфигурации CDN/loader на стороне инфраструктуры)
8	Prefetching	Нет queryClient.prefetchQuery() нигде в коде	При переходе по сделкам/клиентам — видимый loader при каждом переходе	Добавить prefetch onMouseEnter на ссылки деталей сделок, клиентов	✅ Готово (deals/page.tsx и clients/page.tsx; onRowHover добавлен в AppDataTable)

ЧАСТЬ 2: БЭКЕНД (Go 1.24 + PostgreSQL)
Чеклист
#	Модуль / Слой	Описание узкого горла	Последствия	Рекомендация	Статус
9	SMS / Уведомления	sms/usecase.go:141–154 — BulkSend() отправляет SMS синхронно в цикле внутри HTTP-реквеста	При 100 получателях запрос висит несколько секунд; при зависшем SMS-провайдере — timeout для пользователя	Перенести в фоновую очередь (Redis + Asynq или RabbitMQ)	✅ Готово (buffered channel queue, worker.go, BulkSend создаёт pending-логи и ставит в очередь, worker.StartWorker() запускается в go-routine в run.go)
10	Внешние сервисы	Нет Circuit Breaker ни для SMS, ни для будущих payment-провайдеров	Если SMS-провайдер ляжет — все запросы с отправкой уведомлений будут ждать timeout (до 10 сек)	Внедрить sony/gobreaker или circuitbreaker паттерн для внешних HTTP-клиентов	✅ Готово (infrastructure/circuitbreaker/breaker.go + sms/breaker_provider.go; порог 5 отказов, восстановление 30с)
11	Мониторинг	Нет net/http/pprof эндпоинта	В production невозможно профилировать CPU/memory/goroutine leaks без перезапуска сервиса	Добавить pprof на отдельный порт с ограниченным доступом	✅ Готово (PPROF_PORT=6060 запускает debug-сервер на 127.0.0.1, импорт _ "net/http/pprof" в run.go)
12	Трассировка	Нет OpenTelemetry / Jaeger / Zipkin	Невозможно понять, какой репозиторий/usecase тормозит при медленном API	Интегрировать go.opentelemetry.io/otel + экспорт в Jaeger/Grafana Tempo	❌ Не реализовано (требует OTEL-инфраструктуры в prod-окружении)
13	Пагинация	OFFSET/LIMIT во всех репозиториях: client_repository.go:177, schedule_repository.go:271	На OFFSET 50000 PostgreSQL сканирует 50K строк впустую — запрос деградирует до 2–5 сек	Для лент аудита/уведомлений перейти на keyset pagination (WHERE id < $cursor)	⚠️ Приемлемо сейчас, критично при росте
14	Сессии	Сессии хранятся в PostgreSQL (session_repository.go), не в Redis	При высоком RPS (проверка токена на каждый запрос) — лишняя нагрузка на БД	Вынести активные сессии/JWT blacklist в Redis с TTL; PostgreSQL — только для аудита	⚠️ Допустимо для текущего масштаба

ЧАСТЬ 3: МУЛЬТИТЕНАНТНОСТЬ (Tenant Data Isolation)
Чеклист
#	Модуль / Слой	Описание проблемы	Последствия	Рекомендация	Статус
15	Deal Create — кросс-тенантная валидация	deal/usecase.go:Create() не проверяет, что ClientID и ManagerID принадлежат тому же тенанту	Злоумышленник с токеном тенанта A может создать сделку с clientID тенанта B	Добавить ClientExistenceChecker и UserExistenceChecker порты; вызвать перед созданием сделки	✅ Готово (interfaces.go: ClientExistenceChecker + UserExistenceChecker; usecase.go: валидация перед NextDealNumber; run.go: clientExistenceAdapter + userExistenceAdapter)
16	Уникальные индексы без tenant_id	idx_floors_block_number ON floors(block_id, floor_number) и idx_units_property_number ON units(property_id, unit_number) — без tenant_id	Два тенанта не могут иметь этаж №1 в разных блоках с одинаковым block_id (если UUID совпадут не могут, но семантически неверно)	Добавить tenant_id в составной UNIQUE индекс	✅ Готово (000023_tenant_isolation_indexes.sql: DROP + CREATE с tenant_id для floors и units)
17	Фронтенд — очистка кэша при логауте	use-idle-logout.ts: tokenStorage.clearAll() без queryClient.clear()	После авто-логаута по idle следующий пользователь на том же браузере может видеть кэшированные данные	Вызвать queryClient.clear() перед redirect	✅ Готово (useQueryClient() + queryClient.clear() в handleLogout)
18	Фронтенд — инвалидация при смене объекта	property-provider.tsx: setCurrentPropertyId() не инвалидирует кэш TanStack Query	При переключении ЖК отображаются данные предыдущего объекта до следующего refetch	Вызвать queryClient.invalidateQueries() при смене propertyId	✅ Готово (useQueryClient() + void queryClient.invalidateQueries() в setCurrentPropertyId)
19	PostgreSQL RLS	Нет Row-Level Security ни на одной таблице	Если SQL-инъекция или ошибка в запросе — возможна утечка данных другого тенанта	Включить RLS с политикой WHERE tenant_id = current_setting('app.tenant_id')	❌ Не реализовано (требует изменений во всём слое репозиториев и настройки pgx middleware)

ЧАСТЬ 4: AUDIT TRAIL & SOFT DELETE
Чеклист
#	Модуль / Слой	Описание проблемы	Последствия	Рекомендация	Статус
20	AuditRecord — нет old/new value	usecase/common/audit.go: нет полей entity_id, entity_type, old_value, new_value	Нельзя понять, что именно изменилось и каким было значение до изменения	Добавить EntityType, EntityID, OldValue, NewValue в AuditRecord	✅ Готово (audit.go: добавлены EntityType string, EntityID string, OldValue map[string]any, NewValue map[string]any)
21	DeletePayment без audit log	deal/usecase.go:DeletePayment — нет s.audit() после удаления	Кассир может удалить платёж на миллион, в системе не останется никакого следа	Добавить s.audit("payment_deleted") с old_value до удаления	✅ Готово (audit: payment_deleted с EntityType, EntityID, OldValue содержит amount/currency/status/deal_id)
22	EditPayment без audit log	deal/usecase.go:UpdatePayment — нет s.audit() после изменения суммы	Изменение суммы платежа не оставляет следа	Добавить s.audit("payment_edited") с old_value/new_value	✅ Готово (audit: payment_edited с OldValue/NewValue для amount, payment_method, notes)
23	payments — физический DELETE	payment_repository.go:97 — DELETE FROM payments	Запись о платеже безвозвратно уничтожается, дебет с кредитом не сойдётся	Soft delete: UPDATE payments SET deleted_at = NOW()	✅ Готово (querySoftDeletePayment; все SELECT + COUNT добавили AND deleted_at IS NULL; ListByTenant + queryListOverdue + queryCountOverdue тоже)
24	payment_schedule — физический DELETE	schedule_repository.go:81 — DELETE FROM payment_schedule	График платежей уничтожается при перестройке, история недоступна	Soft delete: UPDATE payment_schedule SET deleted_at = NOW()	✅ Готово (DeleteByDealID переписан на soft delete; queryFindScheduleItemByID + queryListScheduleByDeal + ForUpdate + queryUpdateOverdueItems добавили AND deleted_at IS NULL)
25	financial_transactions — физический DELETE	finance_repository.go:428 — DELETE FROM financial_transactions	Удалённая транзакция пропадает из отчётов, баланс не сходится	Soft delete + фильтр в List/Report	✅ Готово (TransactionRepository.Delete→soft delete; List/IncomeExpenseReport/CashFlowReport/PropertyCostReport добавили AND deleted_at IS NULL)
26	expense_categories — физический DELETE	finance_repository.go:371 — DELETE FROM expense_categories	Удалённая категория разрушает исторические отчёты по расходам	Soft delete: UPDATE expense_categories SET deleted_at = NOW()	✅ Готово (ExpenseCategoryRepository.Delete→soft delete; FindByID/FindBySlug/List добавили AND deleted_at IS NULL)
27	Миграция soft delete	Нет столбца deleted_at в payments, payment_schedule, financial_transactions, expense_categories	Soft delete не работает без столбца	Создать миграцию с ALTER TABLE ADD COLUMN deleted_at	✅ Готово (000024_soft_delete_financials.sql: добавляет deleted_at + индексы для всех 4 таблиц)
28	RBAC — бэкенд	RequireRole middleware и защита маршрутов	— 	—	✅ Готово (9 ролей, все чувствительные маршруты /finance /users /settings защищены RequireRole)
29	RBAC — фронтенд	AuthGuard как Server Component	—	—	✅ Готово (finance/layout.tsx, settings/layout.tsx, land/layout.tsx — allowedRoles; прямой URL-доступ невозможен)
30	JWT_SECRET валидация	config.go: минимум 32 символа	—	—	✅ Готово (валидация при старте; docker-compose содержит только dev-значения, .env в .gitignore)
